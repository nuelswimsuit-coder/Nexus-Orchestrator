# Nexus Orchestrator — Worker Requirements
# Install inside the .venv:
#   pip install -r requirements.txt
#
# system_bootstrap.py handles this automatically on first run.

# ── Job queue & broker ────────────────────────────────────────────────────────
arq>=0.26
redis[hiredis]>=5.0
fakeredis>=2.23

# ── Config & data contracts ───────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=24.1
rich>=13.7

# ── System monitoring ─────────────────────────────────────────────────────────
psutil>=6.0
rich>=13.7

# ── Telegram (MTProto client — worker sessions) ───────────────────────────────
telethon>=1.36
PySocks>=1.7.1
cryptography>=42.0
Pillow>=10.0
mutagen>=1.47

# ── Telegram Bot API (notification bot) ──────────────────────────────────────
aiogram>=3.20

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27

# ── Hebrew TTS (swarm voice notes — Edge TTS + ffmpeg → OGG Opus) ─────────────
edge-tts>=6.1

# ── Ops Sync (ahu) — merge nexus_audit_*.xlsx Account Summary into /api/ahu/stats
openpyxl>=3.1.2

# ── Binance websocket (Polymarket bot Linux worker) ───────────────────────────
websockets>=12.0

# ── CCXT — multi-exchange trading library (prediction tasks, Binance feed) ────
ccxt>=4.3

# ── Async utilities ───────────────────────────────────────────────────────────
anyio>=4.4

# ── SSH / remote deploy (Master only, harmless on workers) ───────────────────
paramiko>=3.4

# ── OpenClaw browser scraping (Linux worker only — Phase 19) ─────────────────
# After installing, run: playwright install chromium
playwright>=1.44

# ── Binance websocket velocity (poly5m worker feed) ────────────────────────────
websockets>=12.0

# ── Polymarket CLOB trading (nexus/trading) ───────────────────────────────────
# Official Polymarket Python CLOB client — order placement, book queries
py-clob-client>=0.18
# Builder signing SDK — attaches POLY_BUILDER_* attribution headers to orders
py-builder-signing-sdk>=0.1
