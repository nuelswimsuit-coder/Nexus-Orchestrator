import os, sys, pathlib
root = os.getcwd()
# ????? ?? src ????? ??????
sys.path.insert(0, os.path.join(root, 'src'))
try:
    from nexus.services.session_manager import harvest_sessions
    print("? ???? ?? ????? ????!")
    r = harvest_sessions(vault_dir=pathlib.Path('vault/sessions'), scan_all_drives=True)
    print(f"?? ????! ?????: {r.total_found}, ??????: {r.total_moved}")
except Exception as e:
    print(f"? ?????: {e}")
