/**
 * Default Nexus API origin.
 * Use `http://localhost:8001` (not 127.0.0.1) so the browser origin matches
 * typical dashboard URLs and CORS preflight stays predictable.
 */
export const API_BASE_URL = "http://localhost:8001";
